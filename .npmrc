# 使用 pnpm 作为包管理器
engine-strict=true

# 自动安装 peer dependencies
auto-install-peers=true

# 严格 peer dependencies
strict-peer-dependencies=false

# 提升依赖到根目录（节省空间）
shamefully-hoist=false

# 允许构建脚本运行（包括 esbuild 等）
enable-pre-post-scripts=true

